The program requires python version-> Python 3.10 or later.
Run main.py in order to run the application the 4 other files are imported by it. (So, save all of them in the same directory/folder.)
Required packages:
- numpy
- scipy
- pandas
- matplotlib
